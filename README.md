# scribbit-backend-v3
# Scribbit Backend (FastAPI)

A minimal FastAPI backend intended to run cleanly in **GitHub Codespaces**.

## Quick Start (from scratch)

1. **Create a new repo on GitHub** (add a README to avoid the "empty repo" error).
2. **Add the files from this starter kit** (match the folder structure exactly).
3. Click **Code → Create Codespace on main**.
4. When the container finishes setting up, run:
   ```bash
   ./scripts/start.sh
